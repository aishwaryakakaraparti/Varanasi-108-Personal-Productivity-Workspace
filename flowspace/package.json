{
  "name": "flowspace",
  "version": "2.0.0",
  "description": "FlowSpace — a dev-tool styled personal productivity workspace. Static site, no build step.",
  "private": true,
  "scripts": {
    "start": "npx serve ."
  }
}
